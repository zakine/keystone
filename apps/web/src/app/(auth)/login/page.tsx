export default function LoginPage() {
  return (
    <main className="mx-auto grid min-h-dvh w-full max-w-md content-center px-4">
      <h1 className="text-2xl font-semibold">Sign in</h1>
      <p className="mt-2 text-sm leading-6 text-foreground/70">
        Supabase auth UI and provider-specific flows will attach here when product requirements are
        defined.
      </p>
    </main>
  );
}
